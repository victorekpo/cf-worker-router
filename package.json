{
  "name": "{{ project-name }}",
  "private": true,
  "version": "1.0.0",
  "description": "package for creating workers templates",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "prepare": "husky"
  },
  "author": "{{ authors }}",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.0.11",
    "prettier": "^1.17.0"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "itty-router": "^2.6.6",
    "serverless-cloudflare-workers": "^1.2.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "./mask-account-id.sh"
    }
  }
}
